// Auto-generated wrapper for contract 'TokenLockVault'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/TokenLockVault"
import "@contracts/messages"

struct TokenLockVault {
    address: address
    stateInit: ContractState? = null
}

fun TokenLockVault.fromStorage(
    storage: LockVaultStorage,
    toShard: AddressShardingOptions? = null,
): TokenLockVault {
    val stateInit = ContractState {
        code: build("TokenLockVault"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return TokenLockVault { address, stateInit };
}

fun TokenLockVault.fromAddress(address: address): TokenLockVault {
    return TokenLockVault { address };
}

fun TokenLockVault.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun TokenLockVault.sendDeploy(self, from: address, config: SendParams = {}): SendResultList {
    return self.deploy(from, config);
}

fun TokenLockVault.sendReleaseLock(
    self,
    from: address,
    queryId: uint64,
    positionId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ReleaseLock { queryId, positionId },
    });
    return net.send(from, genericMsg);
}

fun TokenLockVault.sendUpdateTiers(
    self,
    from: address,
    queryId: uint64,
    tierCount: uint8,
    tier0: LockTier,
    tier1: LockTier,
    tier2: LockTier,
    tier3: LockTier,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: UpdateTiers { queryId, tierCount, tier0, tier1, tier2, tier3 },
    });
    return net.send(from, genericMsg);
}

fun TokenLockVault.sendSetEventEnd(
    self,
    from: address,
    queryId: uint64,
    eventEndAt: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetEventEnd { queryId, eventEndAt },
    });
    return net.send(from, genericMsg);
}

fun TokenLockVault.sendSetMinAmounts(
    self,
    from: address,
    queryId: uint64,
    minLockAmount: coins,
    sovereignMinAmount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetMinAmounts { queryId, minLockAmount, sovereignMinAmount },
    });
    return net.send(from, genericMsg);
}

fun TokenLockVault.sendTopUpTons(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUpTons {},
    });
    return net.send(from, genericMsg);
}

fun TokenLockVault.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun TokenLockVault.getLockVaultData(self): (uint64, coins, coins, uint32) {
    return net.runGetMethod(self.address, "get_lock_vault_data");
}

fun TokenLockVault.getPosition(self, positionId: uint64): Cell<LockPosition>? {
    return net.runGetMethod(self.address, "get_position", [positionId]);
}
