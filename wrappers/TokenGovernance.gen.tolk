// Auto-generated wrapper for contract 'TokenGovernance'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/TokenGovernance"
import "@contracts/messages"

struct TokenGovernance {
    address: address
    stateInit: ContractState? = null
}

fun TokenGovernance.fromStorage(
    storage: GovernanceStorage,
    toShard: AddressShardingOptions? = null,
): TokenGovernance {
    val stateInit = ContractState {
        code: build("TokenGovernance"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return TokenGovernance { address, stateInit };
}

fun TokenGovernance.fromAddress(address: address): TokenGovernance {
    return TokenGovernance { address };
}

fun TokenGovernance.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun TokenGovernance.sendDeploy(self, from: address, config: SendParams = {}): SendResultList {
    return self.deploy(from, config);
}

fun TokenGovernance.sendCreateProposal(
    self,
    from: address,
    queryId: uint64,
    descriptionHash: bits256,
    targetContract: address,
    payload: Cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CreateProposal { queryId, descriptionHash, targetContract, payload },
    });
    return net.send(from, genericMsg);
}

fun TokenGovernance.sendCastVote(
    self,
    from: address,
    queryId: uint64,
    proposalId: uint64,
    voteFor: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CastVote { queryId, proposalId, voteFor },
    });
    return net.send(from, genericMsg);
}

fun TokenGovernance.sendExecuteProposal(
    self,
    from: address,
    queryId: uint64,
    proposalId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ExecuteProposal { queryId, proposalId },
    });
    return net.send(from, genericMsg);
}

fun TokenGovernance.sendUpdateConfig(
    self,
    from: address,
    queryId: uint64,
    proposalThreshold: coins,
    quorumBps: uint16,
    votingPeriodSec: uint32,
    timelockSec: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: UpdateConfig { queryId, proposalThreshold, quorumBps, votingPeriodSec, timelockSec },
    });
    return net.send(from, genericMsg);
}

fun TokenGovernance.sendTopUpTons(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUpTons {},
    });
    return net.send(from, genericMsg);
}

fun TokenGovernance.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun TokenGovernance.getConfig(self): Cell<GovernanceConfig> {
    return net.runGetMethod(self.address, "get_config");
}

fun TokenGovernance.getProposal(self, proposalId: uint64): Cell<Proposal>? {
    return net.runGetMethod(self.address, "get_proposal", [proposalId]);
}

fun TokenGovernance.hasVoted(self, proposalId: uint64, voter: address): bool {
    return net.runGetMethod(self.address, "has_voted", [proposalId, voter]);
}
