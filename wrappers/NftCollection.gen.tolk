// Auto-generated wrapper for contract 'NftCollection'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/messages"
import "@contracts/nft-messages"
import "@contracts/nft-storage"

/// Storage `NftCollectionStorage` is defined in `@contracts/nft-storage`
struct NftCollection {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun NftCollection.fromStorage(
    storage: NftCollectionStorage,
    toShard: AddressShardingOptions? = null,
): NftCollection {
    val stateInit = ContractState {
        code: build("NftCollection"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return NftCollection { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun NftCollection.fromAddress(address: address): NftCollection {
    return NftCollection { address };
}

/// Deploys the contract to the blockchain
fun NftCollection.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun NftCollection.sendDeployNftItem(
    self,
    from: address,
    index: uint64,
    owner: address,
    individualContent: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DeployNftItem { index, owner, individualContent },
    });
    return net.send(from, genericMsg);
}

fun NftCollection.sendBatchDeployNftItem(
    self,
    from: address,
    items: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BatchDeployNftItem { items },
    });
    return net.send(from, genericMsg);
}

fun NftCollection.sendChangeCollectionOwner(
    self,
    from: address,
    newOwner: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ChangeCollectionOwner { newOwner },
    });
    return net.send(from, genericMsg);
}

fun NftCollection.sendChangeCollectionContent(
    self,
    from: address,
    newContent: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ChangeCollectionContent { newContent },
    });
    return net.send(from, genericMsg);
}

fun NftCollection.sendGetRoyaltyParams(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GetRoyaltyParams { queryId },
    });
    return net.send(from, genericMsg);
}

fun NftCollection.sendTopUpTons(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUpTons {},
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun NftCollection.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun NftCollection.getCollectionData(self): NftCollectionDataReply {
    return net.runGetMethod(self.address, "get_collection_data");
}

fun NftCollection.getNftAddressByIndex(self, index: int, owner: address, individualContent: cell): address {
    return net.runGetMethod(self.address, "get_nft_address_by_index", [index, owner, individualContent]);
}

fun NftCollection.getNftContent(self, index: int, individualContent: cell): cell {
    return net.runGetMethod(self.address, "get_nft_content", [index, individualContent]);
}

fun NftCollection.royaltyParams(self): RoyaltyParamsReply {
    return net.runGetMethod(self.address, "royalty_params");
}
