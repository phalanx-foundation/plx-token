// Auto-generated wrapper for contract 'MidasHandEscrow'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/MidasHandEscrow"
import "@contracts/messages"

/// Storage `MidasEscrowStorage` is defined in `@contracts/MidasHandEscrow`
struct MidasHandEscrow {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun MidasHandEscrow.fromStorage(
    storage: MidasEscrowStorage,
    toShard: AddressShardingOptions? = null,
): MidasHandEscrow {
    val stateInit = ContractState {
        code: build("MidasHandEscrow"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return MidasHandEscrow { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun MidasHandEscrow.fromAddress(address: address): MidasHandEscrow {
    return MidasHandEscrow { address };
}

/// Deploys the contract to the blockchain
fun MidasHandEscrow.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun MidasHandEscrow.sendDepositTon(
    self,
    from: address,
    queryId: uint64,
    termType: uint8,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DepositTon { queryId, termType },
    });
    return net.send(from, genericMsg);
}

fun MidasHandEscrow.sendApproveByOwner(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ApproveByOwner { queryId },
    });
    return net.send(from, genericMsg);
}

fun MidasHandEscrow.sendClaimAfterUnlock(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ClaimAfterUnlock { queryId },
    });
    return net.send(from, genericMsg);
}

fun MidasHandEscrow.sendRefundTimeout(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RefundTimeout { queryId },
    });
    return net.send(from, genericMsg);
}

fun MidasHandEscrow.sendCancel(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Cancel { queryId },
    });
    return net.send(from, genericMsg);
}

fun MidasHandEscrow.sendTransferNotificationForRecipient(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    transferInitiator: address?,
    forwardPayload: PayloadInline | PayloadInRef,
    config: SendParams = {},
): SendResultList {
    // build body cell manually, because some fields have @abi.clientType
    val bodyB = beginCell()
        .storeUint(0x7362d09c, 32)
        .storeAny(queryId)
        .storeAny(jettonAmount)
        .storeAny(transferInitiator)
        .storeAny(forwardPayload);
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TransferNotificationForRecipient.fromSlice(bodyB.toSlice()),
    });
    return net.send(from, genericMsg);
}

fun MidasHandEscrow.sendTopUpTons(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUpTons {},
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun MidasHandEscrow.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun MidasHandEscrow.getEscrowData(self): (
    uint8,
    uint8,
    coins,
    coins,
    uint32,
    uint32,
) {
    return net.runGetMethod(self.address, "get_escrow_data");
}

fun MidasHandEscrow.getConfig(self): MidasEscrowConfig {
    return net.runGetMethod(self.address, "get_config");
}

fun MidasHandEscrow.getWhaleShareBps(self): uint16 {
    return net.runGetMethod(self.address, "get_whale_share_bps");
}
